// Note-assembly planning — re-exported from @mosaic/sdk (single source of truth; pure logic).
export { spendableNotes, maxIn, planAssembly } from '@mosaic/sdk'
export type { JoinInputRef, AssemblyStep, AssemblyPlan } from '@mosaic/sdk'
