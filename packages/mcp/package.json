{
  "name": "@mosaic/mcp",
  "version": "0.0.0",
  "description": "Authenticated MCP server for Stellar Mosaic. Minimal first release: wallet authentication + the Base->Stellar shield flow (the one piece that genuinely needs a server: RISC Zero/Steel proving + a funded relayer). Everything else runs locally via @mosaic/sdk.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "mosaic-mcp": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "tsc -b && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@mosaic/sdk": "*",
    "@stellar/stellar-sdk": "^16.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "typescript": "~6.0.2"
  }
}
